# Visual Lab asset sources

## Topic diagram

- `diagrams/07-cache-state-cycle.svg`
- Content sources: `docs/theory.md`, `docs/implementation.md`, `docs/checklist.md`
- Visual source: original repository-local diagram created for this Visual Lab; no external image or font was copied.

## Node icons

- `icons/*.svg` are directly renderable exports of the matching outline symbols in `system-icons.svg`.
- `system-icons.svg` is retained as the provenance and backward-compatible symbol source. The current runtime loads `icons/{name}.svg` directly.
- No external icon CDN or icon library is used.
